replica = import_da('replica')
client = import_da('client')


def main():
    replica_process = new(replica.Replica, args=())
    client_process = new(client.Client, args=(replica_process,))
    start(replica_process)
    start(client_process)
