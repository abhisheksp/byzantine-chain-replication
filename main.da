client = import_da('client')
olympus = import_da('olympus')

def main():
    client_process = new(client.Client, args=())
    olympus_process = new(olympus.Olympus, args=(client_process,))
    start(client_process)
    start(olympus_process)
